// Class that allows reading the cache information of the drive and continuing where it left off, if there is a drive
class DriveCacheManager {
  DriveCacheManager._init();

  static DriveCacheManager instance = DriveCacheManager._init();
}
